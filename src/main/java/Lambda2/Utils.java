package Lambda2;

public class Utils {
    public static void printWithSpace(Object obj){
        //this methods prints elements from the same line between space
        System.out.print(obj + " ");

    }
    //this method is going to provide last char of string
    public static char getLastChar(String str){
        return str.charAt(str.length()-1);
        //these classes have static methods used and called directly
        //
    }
}
